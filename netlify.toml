[build]
  base = "/"
  publish = "build/web"
  command = "if cd flutter; then git pull && cd ..; else git clone https://github.com/flutter/flutter.git; fi && flutter/bin/flutter pub get && ./scripts/fix-dependency.sh && flutter/bin/flutter config --enable-web && flutter/bin/flutter build web --release"